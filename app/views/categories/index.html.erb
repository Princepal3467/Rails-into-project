<h1>Categories</h1>

<div class="card-grid">
  <% @categories.each do |category| %>
    <div class="card">
      <h3><%= link_to category.name, category_path(category) %></h3>
      <p><strong>Exercises:</strong> <%= category.exercises.count %></p>
    </div>
  <% end %>
</div>